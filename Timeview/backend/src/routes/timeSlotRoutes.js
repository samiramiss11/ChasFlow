/* const express = require('express');
const { fetchAvailableTimeSlots } = require('../controllers/timeSlotController');
const router = express.Router();

router.get('/', fetchAvailableTimeSlots);  // This route should handle fetching available time slots

module.exports = router;*/
